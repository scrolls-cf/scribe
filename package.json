{
  "name": "scribe",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --port 8791 --ip 127.0.0.1",
    "cf-typegen": "wrangler types",
    "test": "node --experimental-strip-types --test src/**/*.test.ts public/assets/markdown.test.mjs"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250605.0",
    "typescript": "^5.5.2",
    "wrangler": "4.98.0"
  }
}
